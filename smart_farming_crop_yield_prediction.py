# -*- coding: utf-8 -*-
"""Smart Farming Crop Yield Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1hYF4gF68kCNHMJjKCFGMncOit33FvyGz
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

from sklearn.model_selection import train_test_split
from sklearn.preprocessing import LabelEncoder
from sklearn.impute import SimpleImputer
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

df = pd.read_csv('/content/Smart_Farming_Crop_Yield_2024.csv')

print(df.head())
print(df.shape)

df.info()

df.describe()

print(df.isnull().sum())

df.fillna(df.mode().iloc[0], inplace=True)

plt.figure(figsize=(8,5))
sns.histplot(df['yield_kg_per_hectare'], kde=True)
plt.title("Crop Yield Distribution")
plt.show()

plt.figure(figsize=(12,8))
sns.heatmap(df.select_dtypes(include='number').corr(),
            annot=True,
            cmap='coolwarm')
plt.title("Correlation Heatmap")
plt.show()

plt.figure(figsize=(8,5))
sns.boxplot(x='crop_type',
            y='yield_kg_per_hectare',
            data=df)
plt.xticks(rotation=45)
plt.show()

"""Crop produces the highest yield.
Detects variability in yield among crops.
Shows outliers and median yield values
"""

plt.figure(figsize=(8,5))
sns.scatterplot(x='rainfall_mm',
                y='yield_kg_per_hectare',
                data=df)
plt.title("Rainfall vs Crop Yield")
plt.show()

plt.figure(figsize=(8,5))
sns.scatterplot(x='temperature_C',
                y='yield_kg_per_hectare',
                data=df)
plt.title("Temperature vs Crop Yield")
plt.show()

plt.figure(figsize=(8,5))
sns.boxplot(x='irrigation_type',
            y='yield_kg_per_hectare',
            data=df)
plt.title("Irrigation Type vs Yield")
plt.show()

plt.figure(figsize=(8,5))
sns.boxplot(x='fertilizer_type',
            y='yield_kg_per_hectare',
            data=df)
plt.xticks(rotation=45)
plt.title("Fertilizer Type vs Yield")
plt.show()